const Base = require('./base');

module.exports = class extends Base {
  /**
   * @api {GET} /oauth/github  github oauth api
   * @apiGroup  OAuth
   * @apiVersion  0.0.1
   */
  githubAction() {}
};
