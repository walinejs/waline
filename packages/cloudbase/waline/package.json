{
  "name": "express",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "dev": "npm run start",
    "deploy": "cloudbase framework deploy",
    "deploy:server": "cloudbase framework deploy node"
  },
  "dependencies": {
    "@waline/vercel": "0.5.0"
  },
  "devDependencies": {
    "dotenv": "^8.2.0"
  }
}
